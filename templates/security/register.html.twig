{% extends 'base.html.twig' %}

{% block body %}
    <div class="middle-box text-center loginscreen animated fadeInDown">
    <div>
    <div>

        <h2 class="">Bienvenue Chez Entre Nous Comptoir A Pizza</h2>

    </div>
    {{ form_start(form) }}
        <div class="form-group">
        {{ form_row(form.username) }}
        </div>
        <div class="form-group">
        {{ form_row(form.email) }}
        </div>
        <div class="form-group">
        {{ form_row(form.plainPassword.first) }}
        </div>
        <div class="form-group">
        {{ form_row(form.plainPassword.second) }}
        </div>
        <p class="text-muted text-center"><small>Avez-vous un compte ?</small></p>
        <a class="btn btn-sm btn-white btn-block" href="{{ path('connexion') }}">Se connecter</a>
    <button type="submit" class="btn btn-primary block full-width m-b">S'insrcire</button>
    {{ form_end(form)}}

    </div>
    </div>
{% endblock %}